# 本机装有透明加密策略：工作区内的部分源码文件在磁盘上是密文，
# git 直接读取工作区会拿到乱码，导致入库内容不可 diff、不可移植。
#
# 这里给所有文件挂一个 clean 过滤器，由 python 读取文件真实内容后再交给 git。
# python 在加密策略的白名单内，能读到明文。
# 对未被加密的文件，该过滤是恒等操作，不会产生任何副作用。
* filter=plaintext
